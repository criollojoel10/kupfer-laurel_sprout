_mode=cross
pkgname=firmware-sdm670-xiaomi-laurel
pkgver=0.1
pkgrel=1
_arches=specific
arch=(aarch64)
license=(custom:Proprietary)
options=(!strip)
source=(
    firmware-manifest.txt
)
sha256sums=(
    SKIP
)

package() {
    install -Dm644 "$srcdir"/firmware-manifest.txt \
        "$pkgdir"/usr/share/doc/$pkgname/firmware-manifest.txt
}
